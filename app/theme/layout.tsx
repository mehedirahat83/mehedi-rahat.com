import type { Metadata } from "next";
export const metadata: Metadata = { title: "Theme Details", description: "Compare ready website theme packages, included features and setup options." };
export default function Layout({children}:{children:React.ReactNode}){return children}
