import type { Metadata } from "next";
export const metadata: Metadata = { title: "MR News Pro", description: "Discover MR News Pro, a focused publishing solution for modern news websites." };
export default function Layout({children}:{children:React.ReactNode}){return children}
