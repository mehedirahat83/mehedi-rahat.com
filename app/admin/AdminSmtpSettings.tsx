"use client";
import { FormEvent, useEffect, useState } from "react";
import AdminSidebar from "./AdminSidebar";
import "./AdminSmtpSettings.module.css";
type Settings={configured:boolean;host:string;port:number;secure:boolean;username:string;fromEmail:string;fromName:string};
const empty:Settings={configured:false,host:"",port:587,secure:false,username:"",fromEmail:"",fromName:"Mehedi Rahat"};
export default function AdminSmtpSettings(){const [settings,setSettings]=useState<Settings>(empty),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);useEffect(()=>{fetch("/api/admin/smtp-settings",{cache:"no-store"}).then(async r=>{const d=await r.json();if(!r.ok)return window.location.assign("/admin/login");setSettings(d.settings)}).catch(()=>window.location.assign("/admin/login"))},[]);function update<K extends keyof Settings>(key:K,value:Settings[K]){setSettings(current=>({...current,[key]:value}))}async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");setMessage("");const r=await fetch("/api/admin/smtp-settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({...settings,password})});const d=await r.json().catch(()=>null);setBusy(false);if(!r.ok)return setError(d?.error||"Could not save SMTP settings.");setSettings(d.settings);setPassword("");setMessage("SMTP settings saved securely.")}async function test(){setBusy(true);setError("");setMessage("");const r=await fetch("/api/admin/smtp-settings",{method:"POST"});const d=await r.json().catch(()=>null);setBusy(false);if(!r.ok)return setError(d?.error||"SMTP connection failed.");setMessage(d.message)}return <main className="admin-root"><AdminSidebar/><section className="admin-workspace"><header className="admin-topbar"><label><span>⌕</span><input placeholder="Search settings..."/></label><div><span className="admin-top-user">M</span><b>Mehedi Rahat</b></div></header><section className="admin-page-title"><div><span className="eyebrow">Email delivery</span><h1>SMTP Settings</h1><p>Manage password reset and transactional email delivery securely.</p></div></section><form className="admin-card smtp-settings-card" onSubmit={save}><header><div><span className="eyebrow">Primary connection</span><h2>SMTP provider details</h2><p>Passwords remain encrypted and are never shown after saving.</p></div><em className={settings.configured?"smtp-state configured":"smtp-state"}>{settings.configured?"Database settings active":"Environment fallback active"}</em></header><div className="smtp-grid"><label><span>SMTP host</span><input value={settings.host} required onChange={e=>update("host",e.target.value)} placeholder="smtp.example.com"/></label><label><span>SMTP port</span><input type="number" min="1" max="65535" value={settings.port} required onChange={e=>update("port",Number(e.target.value))}/></label><label><span>SMTP username</span><input value={settings.username} required onChange={e=>update("username",e.target.value)} placeholder="email@example.com"/></label><label><span>SMTP password</span><input type="password" value={password} required={!settings.configured} onChange={e=>setPassword(e.target.value)} placeholder={settings.configured?"Leave blank to keep current password":"Enter SMTP password"} autoComplete="new-password"/></label><label><span>From name</span><input value={settings.fromName} required onChange={e=>update("fromName",e.target.value)} placeholder="Mehedi Rahat"/></label><label><span>From email</span><input type="email" value={settings.fromEmail} required onChange={e=>update("fromEmail",e.target.value)} placeholder="info@example.com"/></label></div><label className="smtp-secure"><input type="checkbox" checked={settings.secure} onChange={e=>update("secure",e.target.checked)}/><span><b>Use SSL / secure connection</b><small>Enable for port 465. Keep disabled for TLS on port 587.</small></span></label>{error&&<p className="admin-error-message">{error}</p>}{message&&<p className="premium-order-notice">✓ {message}</p>}<footer><button type="button" onClick={()=>void test()} disabled={busy}>{busy?"Working…":"Test connection"}</button><button type="submit" disabled={busy}>{busy?"Saving…":"Save SMTP settings"}</button></footer></form></section></main>}
