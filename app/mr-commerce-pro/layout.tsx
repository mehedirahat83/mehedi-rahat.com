import type { Metadata } from "next";
export const metadata: Metadata = { title: "MR Commerce Pro", description: "A practical WooCommerce solution built to simplify and grow online business operations." };
export default function Layout({children}:{children:React.ReactNode}){return children}
