import type { Metadata } from "next";
export const metadata: Metadata = { title: "Premium WordPress Products", description: "Browse trusted WordPress plugins, themes and business tools with direct support." };
export default function Layout({children}:{children:React.ReactNode}){return children}
