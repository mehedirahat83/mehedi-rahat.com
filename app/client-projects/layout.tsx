import type { Metadata } from "next";
export const metadata: Metadata = { title: "Client Projects", description: "View selected websites, digital products and business solutions delivered for clients." };
export default function Layout({children}:{children:React.ReactNode}){return children}
