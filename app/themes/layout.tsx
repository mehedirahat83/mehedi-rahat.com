import type { Metadata } from "next";
export const metadata: Metadata = { title: "Ready Website Themes", description: "Browse professionally prepared website themes designed for a faster business launch." };
export default function Layout({children}:{children:React.ReactNode}){return children}
