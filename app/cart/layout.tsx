import type { Metadata } from "next";
export const metadata: Metadata = { title: "Shopping Cart", description: "Review selected digital products, variations and quantities before checkout.", robots: { index: false, follow: false } };
export default function Layout({children}:{children:React.ReactNode}){return children}
